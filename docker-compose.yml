version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: curavia
      POSTGRES_USER: curavia
      POSTGRES_PASSWORD: curavia
    ports: ["5432:5432"]
  redis:
    image: redis:7
    ports: ["6379:6379"]
